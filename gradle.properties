org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError -XX:+UseParallelGC
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true

kotlin.incremental=true
ksp.incremental=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# ── Подпись релиза ──────────────────────────────────────────────
# Ключ лежит в app/release.keystore и закоммичен вместе с проектом:
# терять его нельзя — без него магазин не примет обновление приложения.
AMALIA_KEYSTORE_PASSWORD=plokijuh14231
AMALIA_KEY_ALIAS=kurumi
